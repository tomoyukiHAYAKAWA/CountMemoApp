import SwiftUI

struct AddMemoView: View {
    var body: some View {
        NavigationView {
            Text(/*@START_MENU_TOKEN@*/"Hello, World!"/*@END_MENU_TOKEN@*/)
            .listStyle(PlainListStyle())
            .navigationBarTitle("メモ追加", displayMode: .inline)
        }
    }
}
